"""budai CLI shared library.

This package holds shared utilities for the bin/ scripts. Scripts add
`bin/` to sys.path and import via `from lib.<module> import ...`.

Phase 0 baseline; expanded as scripts mature.
"""

__version__ = "0.1.0"
